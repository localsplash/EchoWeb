services:
  echo-messaging-service:
    image: echo-messaging-service:latest
    build: .
    container_name: echo-messaging-service
    env_file:
      - .env
    expose:
      - "3000"
    volumes:
      - ./inbound:/app/inbound
    restart: unless-stopped
